{
  "name": "@wz/connect-worker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src"
  },
  "dependencies": {
    "@wz/shared": "workspace:*",
    "bullmq": "^5.0.0",
    "dotenv": "^17.4.2",
    "ioredis": "^5.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
